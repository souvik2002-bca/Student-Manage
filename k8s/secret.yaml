apiVersion: v1
kind: Secret
metadata:
  name: sms-secret
type: Opaque
stringData:
  # Base64 encoding is not required for stringData
  MONGO_URI: "mongodb+srv://<username>:<password>@<cluster>.mongodb.net/student-management"
  JWT_SECRET: "your_super_secret_jwt_key_here"
